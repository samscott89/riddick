{
  "name": "@riddick/rust-parser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "cargo install -q worker-build && worker-build --release",
    "deploy": "wrangler deploy",
    "types": "cargo test export_bindings && prettier --write bindings/ && mkdir -p ../../packages/types/src/rust_parser_generated/ && cp bindings/*.ts ../../packages/types/src/rust_parser_generated/"
  }
}
