{
  "name": "@riddick/fixtures",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "generate-fixtures": "node scripts/generate-fixtures.js"
  },
  "dependencies": {
    "@riddick/types": "workspace:*"
  },
  "devDependencies": {
    "@riddick/config": "workspace:*",
    "typescript": "^5.5.3",
    "@types/node": "^20.0.0"
  }
}